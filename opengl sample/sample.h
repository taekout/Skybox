void GL_Draw();

int WIN_GetWidth();  // get window size
int WIN_GetHeight();

int APP_GetCurrentDrawTest();  // this app runs through some opengl tests;  returns 0 to max-draw-tests;  use arrow keys to increment-decrement

float TIME_GetTick();